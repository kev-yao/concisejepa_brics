# @package _global_
defaults:
  - override /model: brics_fsq
  - override /data: brics_bindingdb
  - override /task: brics_jepa
  - override /callbacks: brics_fsq
  - override /trainer: debug

run:
  name_prefix: brics-debug

chem_supervision:
  enabled: false

group_supervision:
  enabled: false

coati_validation:
  enabled: false
